$(".datepicker").datepicker({
            format: 'yyyy-mm-dd',
            //format: 'mm-dd-yyyy',
            autoclose: true,
            orientation: "bottom",
            startView: 'decade',
            minView: 'decade',
            viewSelect: 'decade'
});